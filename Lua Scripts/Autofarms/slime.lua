-- I think this the first for loop I'd ever written!
_G.farm = true
while _G.farm do
while wait() do
    game:GetService("Workspace").Ruichaochen.HumanoidRootPart.CFrame = game:GetService("Workspace").WildernessIsland.Entities.slime.HumanoidRootPart.CFrame
    wait(0.001)
    wait (0.001)
end
end
